version: '3.4'

services:
  marinecadastreaisdataimporterintokafka:
    image: marinecadastreaisdataimporterintokafka
    build:
      context: .
      dockerfile: ./Dockerfile
    ports:
      - 3000:3000
